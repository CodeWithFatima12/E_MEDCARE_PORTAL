# E_MEDCARE_PORTAL
AI Powered E_Medcare Portal Development Code


git status
git add .
git commit -m "update message"
git push


FULL DAILY WORKFLOW (VERY IMPORTANT)
✔ Every day use this cycle:
git status
git add .
git commit -m "update message"
git push
✔ When working with team / GitHub changes:
git pull
👥 4. Friend workflow (important for collaboration)
Friend does:
git clone <repo-url>

Then:

git add .
git commit -m "changes"
git push




hello shifa 
